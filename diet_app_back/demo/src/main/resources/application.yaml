spring:
  datasource:
    url: jdbc:mysql://localhost:3306/diet_app?useSSL=false&serverTimezone=Asia/Tokyo&allowPublicKeyRetrieval=true
    username: diet_user
    password: diet_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
      continue-on-error: true
      schema-locations:
        - optional:classpath:sql/create_users.sql
        - optional:classpath:sql/create_foods.sql
        - optional:classpath:sql/create_snarky_messages.sql
        - optional:classpath:sql/create_food_records.sql
      data-locations:
        - optional:classpath:sql/insert_foods.sql
        - optional:classpath:sql/insert_snarky_messages.sql