# # アプリケーション名
# spring.application.name=diet_app_back

# # サーバーのポート番号（デフォルト: 8080）
# server.port=8080

# # H2 Database 接続設定（メモリ内DBを使用）
# spring.datasource.url=jdbc:h2:mem:dietdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=

# # H2 Console の有効化（ブラウザからDBの中身を確認できるようにする）
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# # JPA/Hibernate の設定（テーブル自動生成・SQLログ出力）
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
